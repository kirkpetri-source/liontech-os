# === Client (Next.js) ===
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
# opcional
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# === Server (Firebase Admin) ===
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# Use \n para quebras de linha, ou rode em uma única linha entre aspas
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nSUA_CHAVE_AQUI\n-----END PRIVATE KEY-----\n"
# opcional (se diferente do padrão <project>.appspot.com)
FIREBASE_STORAGE_BUCKET=

# === Auth (JWT) ===
# Gere com: `openssl rand -base64 32`
AUTH_SECRET=
# Opcional, para proteger rota POST /api/auth/bootstrap
BOOTSTRAP_SECRET=

# === WhatsApp Cloud API (opcional) ===
# Necessário para enviar o PDF da O.S. via WhatsApp
WHATSAPP_TOKEN=
WHATSAPP_PHONE_ID=
# (opcional) Template para iniciar conversa fora da janela de 24h
# Crie no WhatsApp Manager com 3 variáveis: {{1}} número OS, {{2}} status, {{3}} previsão
WHATSAPP_TEMPLATE_NAME=
WHATSAPP_TEMPLATE_LANG=pt_BR

# === Server ===
# Porta local do servidor custom (server.ts)
PORT=3000

# === Prisma (se usar os endpoints com SQLite) ===
# Para desenvolvimento local com SQLite
DATABASE_URL="file:./db/dev.db"

# === Seeds/Admin (opcional) ===
# Usado por scripts/admin-upsert.ts
ADMIN_USUARIO=admin
ADMIN_SENHA=Admin@1234
ADMIN_EMAIL=admin@local
ADMIN_NOME=ADMINISTRADOR